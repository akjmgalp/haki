(function () {
    "use strict";

    var $ = require('jquery');
    //var env = require('node-jsdom').env;

    //describe("Jquery Tests", function () {
    //
    //    it("loads html and parses it as dom", function () {
    //        var options = {host: 'jquery.com', port: 80, path: '/'};
    //
    //        var html = '';
    //        http.get(options, function (res) {
    //            res.on('data', function (data) {
    //                html += data;
    //            }).on('end', function () {
    //                var title = $(html).find('title').text();
    //                console.log(title);
    //            });
    //        });
        //});
    //});

}());